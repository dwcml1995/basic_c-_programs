using System;


namespace structAssignment
{
    public struct Number //defines struct
    {
        public decimal Amount; //properties 

        public Number(decimal amount) //construction that initializies the amount
        {
            Amount = amount;
        }
    }
}
